# Full-folder mirror — track everything (data, videos, checkpoints, plots, pdf,
# zip). Only Python bytecode caches are excluded (auto-regenerated, never useful
# to commit).

__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
